apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - flux-system
  # Bootstrap is managed separately during cluster setup
  # - bootstrap
  # Add other cluster-level resources here if needed
  # - infrastructure
  # - platform 